class CommentData {
  constructor(index, comment, name, url, id) {
    this.index = index;
    this.comment = comment;
    this.name = name;
    this.url = url;
    this.id = id;
  }
}

export default CommentData;
